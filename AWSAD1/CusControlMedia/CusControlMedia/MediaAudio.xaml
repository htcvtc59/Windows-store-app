<UserControl
    x:Class="CusControlMedia.MediaAudio"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CusControlMedia"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="btnOpenFile" Click="btnOpenFile_Click" Content="OpenFile" HorizontalAlignment="Center"/>
            <TextBlock x:Name="tbStatus" HorizontalAlignment="Center"  />
            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnPlay" Click="btnPlay_Click" Content="Play" />
                <Button x:Name="btnPause" Click="btnPause_Click" Content="Pause"/>
                <Button x:Name="btnStop" Click="btnStop_Click" Content="Stop"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
