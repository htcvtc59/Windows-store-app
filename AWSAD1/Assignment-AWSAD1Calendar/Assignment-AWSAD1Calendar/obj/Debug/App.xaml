<Application
    x:Class="Assignment_AWSAD1Calendar.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Assignment_AWSAD1Calendar">
    <Application.Resources>
        <Style TargetType="Button" x:Key="stylebtn">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="styleTb">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
        </Style>

        <Style TargetType="Button" x:Key="ButtonTemplate">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="20,20,20,20" BorderBrush="#A8B3B3" BorderThickness="1.3,1.3,1.3,1.3" >
                            <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" 
                                              Content="{TemplateBinding Content}" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              Margin="{TemplateBinding Padding}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style TargetType="TextBlock" x:Key="StyleMDayN">
            <Setter Property="FontSize" Value="80"/>
            <Setter Property="Foreground" Value="#93A3B0"/>
            <Setter Property="Margin" Value="0,40,0,0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Fonts/Roboto-Bold.ttf#Roboto"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="StyleMainDay">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="#93A3B0"/>
            <Setter Property="Margin" Value="0,20,0,0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Fonts/Roboto-Bold.ttf#Roboto"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="Border" x:Key="BorderDay">
            <Setter Property="BorderBrush"  Value="#A8B3B3"/>
            <Setter Property="BorderThickness"  Value="0,0,1.2,0"/>
            <Setter Property="Opacity"  Value="0.7"/>
        </Style>
        
        <Style TargetType="Border" x:Key="borDay">
            <Setter Property="Width" Value="100"/>
            <Setter Property="BorderBrush" Value="#43D1E3"/>
            <Setter Property="Opacity" Value="1"/>
        </Style>


    </Application.Resources>
</Application>

